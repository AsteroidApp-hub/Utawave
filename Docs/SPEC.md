# Utawave 仕様書

## プロダクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | Utawave |
| ターゲット | 国内の歌い手（ボーカルカバー投稿者） |
| 配布形態 | 無料・オープンソース（AGPL v3） |
| プラットフォーム | Windows / macOS |
| コンセプト | 録音特化・初心者〜中堅フレンドリー |
| Bundle ID | com.utawave.app |
| プロジェクトファイル拡張子 | .uta |
| ドメイン候補 | utawave.com / utawave.com |

---

## 技術スタック

| 項目 | 内容 |
|------|------|
| フレームワーク | JUCE 8.x (C++17) |
| ビルドシステム | CMake |
| ライセンス | AGPL v3 |
| 開発環境（メイン） | macOS + Xcode |
| Windows ビルド | UTM（仮想マシン）+ GitHub Actions |
| バージョン管理 | Git / GitHub (https://github.com/AsteroidApp-hub/Utawave) |
| 内蔵 MP3 エンコーダ | LAME (静的リンク) |
| リサンプラー | r8brain-free-src (MIT) |
| ピッチシフト | 自社製 PitchCore (フェーズボコーダ + r8brain, Source/Audio/pitchcore/) |

---

## 画面構成

```
┌──────────────────────────────────────────────────────────────────┐
│  ツールバー（Transport / BPM / タイム / タップテンポ / メトロノーム） │
├────────────────────────┬─────────────────────────┬───────────────┤
│                        │                         │               │
│  トラックヘッダー       │   タイムライン           │  マスター      │
│  ・フェーダー + Pan    │   ・クリップ表示         │  パネル        │
│  ・Rev（リバーブ送り）  │   ・レーン表示           │               │
│  ・VU / Peak メーター  │   ・マーカー表示         │               │
│  ・INS スロット (×4)   │   ・2段ルーラー          │               │
│  ・M/S/R/I             │   ・ピアノロール起動可    │               │
│                        │                         │               │
├────────────────────────┴─────────────────────────┴───────────────┤
│  ステータスバー（CPU% / プロジェクト情報 / 総尺）                  │
└──────────────────────────────────────────────────────────────────┘
```

---

# v1.0 リリース対象機能（実装済み）

## 1. 録音

| 機能 | 仕様 |
|------|------|
| マルチトラック録音 | mono / stereo・入力チャンネル選択 |
| レーン録音 | テイクごとに Take レーンへ自動バックアップ |
| ループ録音 | LOOP 範囲設定 + Rec で自動 |
| パンチイン録音 | 再生中に R で開始 → 既存クリップ上にオーバーレイ録音 |
| 遡及録音 | 再生中バックグラウンドキャプチャ → Cmd+Shift+R で確定 |
| プリロール | 0 / 1 / 2 / 3 秒 |
| カウントイン | 0 / 1 / 2 / 4 小節 |
| メトロノーム | 専用 CLICK トラック・Beep / Stick / Cowbell / Wood の 4 種類・1 拍目アクセント・Half / Normal / Double レート |
| タップテンポ | ツールバーの TAP ボタン |
| 入力レベルメーター | 録音前に Rec アーム or Input Monitor で確認 |
| テイク比較 | Take レーン (Shift+↑ で Lane 0 へ採用) |
| 自動リサンプリング | 異なる SR の WAV を r8brain で変換 |
| パンチイン境界クロスフェード | 自動 30ms フェード (クリック防止) |

> 複数トラック同時録音に対応 (Rec アーム済みの全トラックを別 input channel から並行記録)

## 2. 編集

| 機能 | 仕様 |
|------|------|
| クリップ操作 | 移動・リサイズ・分割 (Option/Alt+Click)・結合 (Consolidate) |
| フェード | Linear / Logarithmic / EqualPower / SCurve |
| クロスフェード | F = 単独 fade / X = 隣接 xfade・両端ハンドルでサイズ調整 |
| クリップゲイン | フェーダー + ブレークポイントエンベロープ |
| 無音区間カット | Strip Silence |
| BPM 検出 | 波形右クリック → 「テンポを検出」 |
| ピッチシフト | -6〜+6 半音・キャッシュ付き (Audio フォルダ内に保存) |
| Undo / Redo | JUCE UndoManager (LaneSnapshotAction で複雑操作も網羅) |
| スナップ | Off / 1/1 / 1/2 / 1/4 / 1/8 / 1/16 / 1/32 / 1/4T / 1/8T / 1/16T |
| クリップ名称変更 | 各クリップに任意の名前 |
| クリップカラーリング | 8 色から選択 |
| ナッジ | 矢印キーで微小移動 |
| スマートツール | 上半分=範囲選択 / 下半分=ハンド・自動切替 |
| 自動レーン退避 | ドラッグ重ね時、下側を Take レーンへ自動移動 |

## 3. ミキサー（トラック上）

| 機能 | 仕様 |
|------|------|
| フェーダー | 横型スライダー (Vol) |
| Pan | スライダー (中央 = 0) |
| Rev | 簡易リバーブセンド (0..1) |
| VU / Peak メーター | IEC 268-17 準拠 300ms 積分・SR/blockSize 非依存・ピークホールド付き |
| ミュート / ソロ / Rec アーム / Input Monitor | トラックヘッダの M/S/R/I |
| INS スロット | 固定 4 枠 (VST3)・自己管理エディタウィンドウ |
| INS D&D | スロット間移動・Option+Drag コピー (別トラック間も可) |
| INS バイパス | Cmd+Click で個別バイパス |
| PDC | プラグインディレイ補正 (TrackDelay 構造体) |
| トラック複製 | 右クリックメニュー「トラックを複製」(クリップ・設定・INS をクローン) |

## 4. マスターパネル（右固定）

| 機能 | 仕様 |
|------|------|
| マスターバスインサート | 4 枠 (リミッター・EQ 等) |
| DITHER スロット | 24bit 未満出力時のみ表示 |
| マスターフェーダー | dB スケール |
| VU / Peak メーター | L/R ステレオ・ピークホールド |
| 折りたたみ | パネル全体を閉じてタイムラインを広く取れる |
| CPU メーター | ステータスバーに % 表示 |

## 5. MIDI

| 機能 | 仕様 |
|------|------|
| SMF 読み込み | .mid / .midi |
| トラック選択ダイアログ | チェックでインポート対象を選択 |
| マーカー / テンポ取り込み | チェックボックスで ON/OFF |
| 内蔵シンセ | Sine / Saw / Square + Attack-Hold-Release エンベロープ |
| 内蔵シンセ ON/OFF | OFF にすると MIDI が INS チェーン (VST 音源) へ直接送られる |
| オクターブシフト | ±4 オクターブ |
| 半音トランスポーズ | ±12 半音 |
| ソロ連動 | MIDI トラックも含めて連動 |

### ピアノロールエディタ（MIDI クリップ ダブルクリックで起動）

| 操作 | 機能 |
|------|------|
| ダブルクリック (空き) | ノート新規追加 |
| ドラッグ (ノート上) | 移動 |
| ドラッグ (端) | リサイズ |
| Cmd+Click | 複数選択・範囲選択 |
| Cmd+A / C / X / V | 全選択 / コピー / カット / ペースト |
| Cmd+Z / Cmd+Shift+Z | Undo / Redo (内部 stack) |
| Delete / Backspace | 選択ノート削除 |
| ← / → | ノートを時間方向にナッジ |
| ↑ / ↓ | ノートを半音上下 |
| Velocity バー | 個別ベロシティ調整 |
| グリッド設定 | Shift+0〜9 (メインと連動) |
| 再生バー同期 | メインタイムラインと同期 |

## 6. 書き出し

| 機能 | 仕様 |
|------|------|
| フォーマット | WAV / AIFF / MP3 (内蔵 LAME) |
| ビット深度 | 16 / 24 / 32-float |
| 範囲 | プロジェクト全体 / 範囲選択 / 小節範囲 (開始〜終了小節を数値指定) |
| 出力モード | マスタ書き出し / ステム書き出し (全トラック個別) |
| Pre / Post フェーダー | デフォルト Pre |
| ピーク超過保護 | 環境設定で ON/OFF (デフォルト ON)・±0.989 超で全体減衰 |
| 高品質ディザー | HP-TPDF + 9-tap ノイズシェイピング (24bit 未満時自動) |
| MP3 INFO/Xing タグ | 書き戻しで尺・シーク精度を確保 |
| メタデータ埋め込み | なし |

## 7. 音源読み込み

| 機能 | 仕様 |
|------|------|
| ドラッグ&ドロップ | Finder / Explorer から直接タイムラインへ |
| メニュー / Cmd+I | 「ファイル」→「オーディオを読み込む...」 |
| 配置位置 | インポートしたクリップは常にプロジェクト先頭 (0 秒) に配置 |
| 自動リサンプリング | プロジェクト SR と異なる WAV を r8brain で変換 |
| iXML / bext / smpl 削除 | デフォルト ON (設定で OFF 可)。他 DAW のテンポ情報流入を防ぐ |
| ラウドネス自動調整 | LUFS ターゲットに合わせてトラックゲインを設定 (設定で OFF 可) |
| Audio フォルダへのコピー | インポートしたファイルは必ず `<projectFolder>/Audio/` にコピー (元ファイルは変更しない) |

## 8. VST (プラグイン)

| 機能 | 仕様 |
|------|------|
| 対応形式 | VST3 (macOS / Windows) |
| スキャン | 起動時自動 + 手動 |
| スキャンパス | OS デフォルトパスを自動検索・カスタムパス追加可 |
| Apple Silicon | ネイティブ対応 |
| プラグインエディタ | 自己管理ウィンドウ |

## 9. プロジェクト管理

| 機能 | 仕様 |
|------|------|
| プロジェクトファイル | .uta (XML)・音声参照は相対パス |
| 新規 / 開く / 保存 / 別名で保存 | 標準操作 |
| 最近使用プロジェクト | 起動画面とファイルメニューから |
| 自動保存 | 5 / 10 / 15 / 20 / 25 / 30 分から選択 (Backup/ 配下) |
| クラッシュ復旧 | 起動時に autosave が本体より新しければ復旧ダイアログ |
| 未保存確認 | 新規・開く・閉じる・ウィンドウ ✕ ボタン経由で確認ダイアログ |
| アトミック保存 | tmp ファイル → replaceFileIn で書き込み中クラッシュに耐性 |
| 欠損ファイル警告 | ロード時に参照先が見つからないクリップを検出しダイアログで一覧表示 (再リンク UI は v1.1) |
| トラック複製 | 右クリックメニューから (プラグイン状態もコピー) |

### プロジェクトフォルダ構造

```
MyProject/
├── MyProject.uta        # XML プロジェクト
├── Audio/                   # 録音 / インポート音声
├── Cache/                   # 波形キャッシュ等
├── Backup/                  # 自動保存
└── Export/                  # 書き出し既定先
```

## 10. UX / UI

| 機能 | 仕様 |
|------|------|
| テーマ | ダークテーマ固定 |
| 2 段ルーラー | 時刻行 / 小節行を独立に表示 ON/OFF |
| ルーラークリックでシーク | マーカー行以外の全行 (テンポ / 拍子 / 小節 / 時刻) の左クリックで再生位置を移動 |
| テンポマップ | BPM・拍子の途中変更対応 |
| マーカー | N / B で次/前へジャンプ |
| ループ範囲 | LOOP ボタンで設定 (再生用。書き出し範囲には非対応) |
| 横ズーム | Cmd+scroll (1〜200,000 px/beat) |
| 縦ズーム | Shift+Option+scroll |
| プロジェクト全体ビュー | Shift+F |
| メニューバー | macOS グローバル / Windows ウィンドウ上端 |
| ヘルプ | About / ショートカット一覧 (Help メニュー) |
| ステータスバー | CPU% / プロジェクト情報 / 総尺 |
| 起動画面 | 新規 / 開く / 最近のプロジェクト |

## 11. デフォルトショートカット

| キー | 機能 |
|------|------|
| `Space` | 再生 / 停止 |
| `R` | 録音開始 / 停止 (パンチイン対応) |
| `S` | 停止 (Shift+S で選択トラックのソロ) |
| `M` | 選択トラック ミュート (Shift+M) |
| `I` | 入力モニター (Shift+I) |
| `T` | TList (Take レーン) トグル |
| `Cmd+Z` / `Cmd+Shift+Z` | Undo / Redo |
| `Cmd+S` / `Cmd+Shift+S` | 保存 / 別名で保存 |
| `Cmd+O` | 開く |
| `Cmd+I` | オーディオファイル取り込み |
| `Cmd+A/C/V/X/D` | 全選択 / コピー / ペースト / カット / 複製 |
| `Cmd+E` | 書き出し |
| `Cmd+Shift+R` | 遡及録音を確定 |
| `Cmd+,` | 環境設定 |
| `Option+クリック` (Alt) | クリップ分割 (波形 / MIDI) |
| `Cmd+クリック` | プラグインバイパス |
| `Cmd+スクロール` | 横ズーム |
| `Shift+Option+スクロール` | 波形振幅縦ズーム |
| `Shift+F` | プロジェクト全体ビューに合わせる |
| `←` / `→` | クリップナッジ |
| `Shift+↑` | Take レーンから Lane 0 へ採用 |
| `F` | 選択範囲で fade-in / fade-out |
| `X` | 選択範囲でクロスフェード |
| `N` / `B` | 1 小節 進む / 戻る |
| `Shift+N` / `Shift+B` | 次 / 前のマーカーへジャンプ |
| `;` / `-` | 再生バーを進む / 戻る (押しっぱなしで滑らかに移動) |
| `L` | ループ再生 ON / OFF |
| `Shift+0〜9` | グリッド切替 |
| `Delete` / `Backspace` | 選択クリップ / クロスフェード削除 |

---

# v1.1 以降の予定（保留）

将来的に実装したいが現バージョンでは保留している機能。

### ミキシング
- **フェーズ反転** (トラック単位の極性反転)
- **トラックグループ** (M/S/Vol を複数トラックで連動)
- **インサート一括バイパス** (トラックの全インサートをワンボタン)
- **プラグインお気に入り** (★ マークでフィルタ)
- **プリセットブラウザ** (DAW 内から VST プリセットを一覧・読込)
- **メーター折り畳み**
- **フェーダーノブ切替**
- **Pan ロー選択** (-3 / -6 / 0 dB)

### 録音
- **ヘッドフォンモニターミックス** (録音者用に独立したモニター送出)
- **録音直後の無音自動トリム**
- **カスタムメトロノーム音源** (任意 WAV)
- **メトロノーム音価選択** (4分 / 8分 / 16分 の明示 UI)

### 編集
- **タイムラインクリッピング表示** (音割れクリップを赤ハイライト)

### 書き出し
- **バッチ書き出し** (複数マーカー範囲を一括)
- **右クリック書き出し** (クリップ選択範囲をメニューから)

### 音源読み込み
- **動画から音声抽出** (MP4 / MOV から音声トラック自動抽出)

### VST
- **プラグインブラックリスト** (問題のある VST を除外)
- **セーフモード起動** (VST 読み込みをスキップ)
- **スキャンパス追加 UI**

### プロジェクト
- **プロジェクトメモ** (歌詞・指示などのテキスト添付)
- **テンプレートプロジェクト** ("ボーカル+伴奏" などのひな形)
- **欠損ファイルの再リンク UI** (起動時の警告自体は v1.0 で実装済み。ファイルを選び直して
  参照を貼り替える対話 UI が v1.1 対象)
- **バックアップ世代数** (現状は 1 ファイル上書き)

### UX
- **ショートカットカスタマイズ**
- **レイテンシ設定ウィザード** (初回起動時の自動測定)
- **スクロールフォロー** (再生時のプレイヘッド自動追従)
- **トラック高さプリセット** (S / M / L)
- **トラックパッドピンチズーム**
- **マーカークイックジャンプ** (0〜9 数字キー)
- **ズーム / ウィンドウ状態の完全記憶**

---

# 不採用

検討の結果、Utawave としては実装しないと決めた項目。

| 項目 | 理由 |
|------|------|
| **VST2 対応** | JUCE 8 で SDK サポートが廃止された |
| **64-float bit 書き出し** | 実用上の需要なし。32-float で十分 |
| **内蔵ファイルブラウザ** | OS のファイラ + D&D で十分 |
| **Collect All** | インポート時点で常に `<projectFolder>/Audio/` へコピーするため既に集約済み |
| **MIDI インスペクターウィンドウ** | ピアノロールエディタが完全な編集機能を提供するため不要 |
